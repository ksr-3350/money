# 📱 아이폰에 재무컨설턴트 앱 설치하기

앱스토어 없이, **PWA(홈 화면 웹앱)** 방식으로 설치합니다.
홈 화면 아이콘 💰 을 누르면 주소창 없는 전체화면 앱처럼 실행됩니다.

---

## 방법 1. GitHub Pages 무료 호스팅 (추천 — 어디서나 접속, 오프라인 지원)

> 한 번만 해두면 평생 무료로 쓸 수 있습니다. 10분 소요.

### 1단계. GitHub 가입
- https://github.com 접속 → **Sign up** → 이메일로 가입

### 2단계. 저장소(repository) 만들기
1. 로그인 후 우측 상단 **+** → **New repository**
2. Repository name: `money` (원하는 이름 아무거나)
3. **Public** 선택 → **Create repository**

### 3단계. 앱 파일 업로드
1. 만들어진 저장소 화면에서 **"uploading an existing file"** 링크 클릭
2. 이 폴더의 아래 **6개 파일을 전부 드래그**해서 업로드:
   - `index.html`
   - `manifest.json`
   - `sw.js`
   - `icon-180.png`
   - `icon-192.png`
   - `icon-512.png`
3. 하단 **Commit changes** 클릭

### 4단계. Pages 켜기
1. 저장소 상단 **Settings** → 왼쪽 메뉴 **Pages**
2. Branch: **main** 선택 → **Save**
3. 1~2분 후 상단에 주소가 나타남:
   `https://내아이디.github.io/money/`

### 5단계. 아이폰에 설치
1. 아이폰 **Safari**에서 위 주소 접속
2. 하단 가운데 **공유 버튼(⬆️)** 탭
3. **"홈 화면에 추가"** 선택 → **추가**
4. 끝! 홈 화면의 💰 아이콘으로 실행

> ⚠️ 앱 코드만 공개될 뿐, **내 자산 데이터는 절대 업로드되지 않습니다.**
> 데이터는 각 기기(아이폰/PC) 안에만 저장됩니다.

---

## 방법 2. 같은 Wi-Fi에서 바로 테스트 (PC가 켜져 있을 때만)

1. PC에서 PowerShell 실행 후:
   ```
   cd "C:\Users\Administrator\Documents\클로드 프로젝트\주식부자\재무컨설턴트"
   python -m http.server 8741
   ```
2. PC의 IP 확인: `ipconfig` → IPv4 주소 (예: 192.168.0.10)
3. 아이폰 Safari에서 `http://192.168.0.10:8741` 접속
4. 공유(⬆️) → "홈 화면에 추가"

> 이 방식은 PC가 꺼지면 접속이 안 되고, 오프라인 모드도 동작하지 않습니다.
> 정식 사용은 방법 1을 권장합니다.

---

## 💾 PC ↔ 아이폰 데이터 옮기기

데이터는 기기별로 따로 저장됩니다. 옮기려면:
1. 옮길 쪽 기기에서 **설정 탭 → 백업 파일 다운로드(JSON)**
2. 파일을 카카오톡/메일 등으로 전송
3. 받는 기기에서 **설정 탭 → 백업 파일 불러오기**

---

## ❓ 자주 묻는 질문

- **앱스토어 등록은 안 되나요?** → Mac + Xcode + 애플 개발자 계정(연 $99)이 필요해서 개인용으로는 PWA가 현실적입니다. 사용감은 거의 동일합니다.
- **시세 갱신이 가끔 실패해요** → 무료 시세 API(Yahoo) + 무료 프록시를 사용하므로 일시적으로 실패할 수 있습니다. 잠시 후 🔄 버튼을 다시 누르면 됩니다.
- **알림이 오나요?** → 앱을 열 때 이번 달 자산 업데이트를 안 했으면 상단에 배너로 알려줍니다. (iOS 푸시 알림은 별도 서버가 필요해서 미지원)
